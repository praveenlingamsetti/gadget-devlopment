import type { AmbientContext } from "./AmbientContext";
import type { ActionTrigger, ActionExecutionScope } from "./types";

/** Context of the `updateInventoryToZero` action. */
export interface InventoryUpdateInventoryToZeroGlobalActionContext extends AmbientContext {
  /**
  * @deprecated Use 'returnType' instead.
  * Useful for returning data from this action by setting `scope.result`.
  */
  scope: ActionExecutionScope;
  /**
  * An object specifying the trigger to this action (e.g. API call, custom params).
  */
  params: {
};
  /**
  * An object specifying the trigger to this action (e.g. api call, scheduler etc.)
  */
  trigger: ActionTrigger;
  /**
  * @private The context of this action.
  */
  context: InventoryUpdateInventoryToZeroGlobalActionContext;
};


/** Context of the `readOrderLineItems` action. */
export interface OrderReadOrderLineItemsGlobalActionContext extends AmbientContext {
  /**
  * @deprecated Use 'returnType' instead.
  * Useful for returning data from this action by setting `scope.result`.
  */
  scope: ActionExecutionScope;
  /**
  * An object specifying the trigger to this action (e.g. API call, custom params).
  */
  params: {
};
  /**
  * An object specifying the trigger to this action (e.g. api call, scheduler etc.)
  */
  trigger: ActionTrigger;
  /**
  * @private The context of this action.
  */
  context: OrderReadOrderLineItemsGlobalActionContext;
};


/** Context of the `getVariantsByproduct` action. */
export interface ProductGetVariantsByproductGlobalActionContext extends AmbientContext {
  /**
  * @deprecated Use 'returnType' instead.
  * Useful for returning data from this action by setting `scope.result`.
  */
  scope: ActionExecutionScope;
  /**
  * An object specifying the trigger to this action (e.g. API call, custom params).
  */
  params: {
};
  /**
  * An object specifying the trigger to this action (e.g. api call, scheduler etc.)
  */
  trigger: ActionTrigger;
  /**
  * @private The context of this action.
  */
  context: ProductGetVariantsByproductGlobalActionContext;
};


/** Context of the `readMetafieldByProduct` action. */
export interface ProductReadMetafieldByProductGlobalActionContext extends AmbientContext {
  /**
  * @deprecated Use 'returnType' instead.
  * Useful for returning data from this action by setting `scope.result`.
  */
  scope: ActionExecutionScope;
  /**
  * An object specifying the trigger to this action (e.g. API call, custom params).
  */
  params: {
};
  /**
  * An object specifying the trigger to this action (e.g. api call, scheduler etc.)
  */
  trigger: ActionTrigger;
  /**
  * @private The context of this action.
  */
  context: ProductReadMetafieldByProductGlobalActionContext;
};


/** Context of the `readProductBySwatchId` action. */
export interface ProductReadProductBySwatchIdGlobalActionContext extends AmbientContext {
  /**
  * @deprecated Use 'returnType' instead.
  * Useful for returning data from this action by setting `scope.result`.
  */
  scope: ActionExecutionScope;
  /**
  * An object specifying the trigger to this action (e.g. API call, custom params).
  */
  params: {
};
  /**
  * An object specifying the trigger to this action (e.g. api call, scheduler etc.)
  */
  trigger: ActionTrigger;
  /**
  * @private The context of this action.
  */
  context: ProductReadProductBySwatchIdGlobalActionContext;
};


/** Context of the `updateProductStatus` action. */
export interface ProductUpdateProductStatusGlobalActionContext extends AmbientContext {
  /**
  * @deprecated Use 'returnType' instead.
  * Useful for returning data from this action by setting `scope.result`.
  */
  scope: ActionExecutionScope;
  /**
  * An object specifying the trigger to this action (e.g. API call, custom params).
  */
  params: {
};
  /**
  * An object specifying the trigger to this action (e.g. api call, scheduler etc.)
  */
  trigger: ActionTrigger;
  /**
  * @private The context of this action.
  */
  context: ProductUpdateProductStatusGlobalActionContext;
};


